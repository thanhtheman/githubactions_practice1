name: deploy
on: [push]
jobs:
    # lint:
    #     runs-on: ubuntu-latest
    #     steps:
    #         - uses: actions/checkout@v3
    #         - run: npm ci
    #         - run: npm run lint
    tests:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - run: npm ci
            - run: npm run test
    deploy:
        runs-on: ubutun-latest
        steps:
            - uses: actions/checkout@v3
            - run: npm ci
            - run: npm run build
            - run: echo "Deploying..."